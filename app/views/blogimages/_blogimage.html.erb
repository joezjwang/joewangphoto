<!-- create action will use this partial while an indentical partial exist in the admin folder since
when index view is loaded it looks for the partial in admin folder. what a mess and it should be fixed -->
<div class="blogimage" id="<%= dom_id(blogimage) %>">
  <%= image_tag blogimage.image.url(:pc_small), :height => '140', class: "blogimage-preivew" %>
  <div class="title"><%= blogimage.title %> [<%= blogimage.id %>]</div>
  <div class="caption"><%= blogimage.caption %></div>
  <div class="actions">
    <%= link_to 'Edit', edit_admin_blogimage_path(blogimage), class: "pure-button", target: "_blank" %>
    <%= link_to 'Destroy', admin_blogimage_path(blogimage), method: :delete, data: { confirm: 'Are you sure?' }, class: "pure-button delete-blogimage", remote: true %>
  </div>
</div>