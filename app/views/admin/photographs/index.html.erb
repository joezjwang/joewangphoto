<div class="pure-g">
  <div class="pure-u-4-24"></div>
  <div class="pure-u-18-24">
    <p id="notice"><%= notice %></p>

    <h1>Listing Photographs</h1>

    <table class="pure-table pure-table-horizontal">
      <thead>
        <tr>
          <th>Id</th>
          <th style="max-width:100px;">Title</th>
          <th>Location</th>
          <th style="max-width:400px;">Description</th>
          <th>Collection</th>
          <th>Preview</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @photographs.each do |photograph| %>
          <tr>
            <td><%= photograph.id %></td>
            <td style="max-width:100px;"><%= photograph.title %></td>
            <td><%= photograph.location %></td>
            <td style="max-width:400px;"><%= photograph.description %></td>
            <td>
            <% unless photograph.collections.empty? %>
                <% photograph.collections.each do |collection| %>
                <%= link_to collection.name, admin_collection_path(collection) %><br>
                <% end %>
              <% end %>
            </td>
            <td>
              <%= image_tag photograph.photo.url(:thumb), :height => '60'%>
            </td>
            <td><%= link_to 'Show', admin_photograph_path(photograph), class: "pure-button" %></td>
            <td><%= link_to 'Edit', edit_admin_photograph_path(photograph), class: "pure-button" %></td>
            <td><%= link_to 'Destroy', admin_photograph_path(photograph), method: :delete, data: { confirm: 'Are you sure?' }, class: "pure-button" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

  </div>
  <div class="pure-u-2-24"></div>
</div>