<%= form_for @product, :html => {class: 'pure-form' } do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: 'pure-input-2-3' %>
  </div>
  <div class="field">
    <%= label_tag "Paste Shopify embed code:" %><br>
    <%= text_area_tag 'shopify_embed_code', nil, cols: 100, rows: 20 %>
  </div>
  <div class="field">
   <%#= f.file_field :photo %>
  </div>
  <br><br>
  <div class="actions">
    <%= f.submit class: 'pure-button pure-button-primary' %>
  </div>
  <br><br>
<% end %>

